import type { PostDef } from '../../../interfaces'

/**
 * STUB — a reserved slot in the journey. Promote by filling the AEO fields and removing `draft`.
 * Anchor: same workflow — ~1/3 of nodes only run when something breaks
 */
export const automationIsMostlyFailureHandling: PostDef = {
	id: 'automation-is-mostly-failure-handling',
	title: 'Most of my automation is failure handling',
	publishedAt: '2026-08-18',
	chapter: 'ownership',
	draft: true
}
